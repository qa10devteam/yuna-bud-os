[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "terra-documents"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["terra-ai"]

[tool.setuptools.packages.find]
where = ["."]
include = ["services.documents*"]
