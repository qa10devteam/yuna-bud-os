# ── terra.os UI — environment variables ──────────────────────────────────────
# Copy to .env.local (development) or set as CI/Vercel variables (production).

# API base URL — leave empty to use relative path (same-origin proxy recommended)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8765
